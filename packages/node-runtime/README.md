# @lumen-lang/node

The Node runtime package specs 503–508 target. `spike/` is spec 508's
prototype (kept as a record of what it found, not a package entry point);
the real runtime library lands here per specs 503's tasks.md.
